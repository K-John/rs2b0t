export const QuestFood = { name: 'Trout' as string | null };

/**
 * Why: the float a quest carries is 8 unless it proves it needs more, the old 12-to-20 packs spent most of the inventory on food that was never eaten, and left nothing for quest items.
 */
export const FOOD_FLOAT = 8;
